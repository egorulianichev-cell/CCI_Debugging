version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:2026.03
    resource_class: small

jobs:
  # php-test: 
  #   executor: php
  #   steps:
  #     - run:
  #         name: php extract script
  #         command: |
  #           sudo docker-php-source extract
  #     - run:
  #         name: run php docker php-test
  #         command: |
  #           sudo docker-php-ext-install zip bcmath pdo_mysql

  new_test_job:
    docker:
      - image: ${AWS_ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com/ci_images/circleci-runner:1.6
    steps:
      - run: env | sort
workflows:
  docker-check-workflow:
    jobs:
      - aws_acc_id