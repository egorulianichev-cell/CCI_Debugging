version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:2026.03
    resource_class: small

orbs:
    jq: circleci/jq@3.0.0

jobs:
  install-jq:
    executor: base
    steps: 
      - jq/install:
          version: jq-1.7


workflows:
  test-jq-install:
    jobs: 
      - install-jq